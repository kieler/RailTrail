import { StyleSheet } from "react-native"

export const textStyles = StyleSheet.create({
  headerTextBig: {
    fontWeight: "600",
    fontSize: 18,
  },
  headerTextNormal: {
    fontWeight: "600",
    fontSize: 14,
  },
})
